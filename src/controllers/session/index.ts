import { Request, Response, NextFunction } from "express";

export const createSessionController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const getSessionInfoController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const getSessionsController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
