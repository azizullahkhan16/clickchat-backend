import { NextFunction, Request, Response } from "express";

export const addLeadController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const getLeadsController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const getLeadInfoController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const updateLeadController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const deleteLeadController = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
